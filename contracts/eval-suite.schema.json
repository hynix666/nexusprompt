{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://promptnexus.dev/contracts/eval-suite/2.0.1",
  "title": "EvalSuite",
  "description": "A named, versioned set of cases with a declared job. The kind field is load-bearing: a suite small enough to run in seconds cannot certify a promotion, and a suite large enough to certify one cannot gate every change. Conflating them yields either a suite nobody runs or a promotion nobody can justify.",
  "type": "object",
  "required": [
    "suite_id",
    "version",
    "kind",
    "case_ids",
    "resolution",
    "significance_protocol"
  ],
  "additionalProperties": false,
  "properties": {
    "suite_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "description": "smoke gates every change and must run offline in seconds. anchor alone may certify a promotion and must satisfy the sizing requirement below. adversarial holds seeded perturbations of another suite.",
      "enum": [
        "smoke",
        "anchor",
        "adversarial"
      ]
    },
    "case_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "resolution": {
      "description": "The smallest delta this suite will REPORT -- its score granularity, one case out of n. It is not the suite's statistical resolution and 2.0.0's description wrongly conflated the two by quoting the sizing rule here. Two different floors apply to a comparison and both are enforced: a delta below granularity is arithmetically meaningless, and a comparison with fewer discordant pairs than the exact test's support admits is inferentially meaningless. The second is DERIVED by the comparator from alpha and the observed discordance, never declared, because a declared statistical resolution is exactly the number that drifted.",
      "type": "object",
      "required": [
        "detectable_delta",
        "confidence"
      ],
      "additionalProperties": false,
      "properties": {
        "detectable_delta": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1,
          "description": "Score granularity: one case out of n. The minimum delta worth reporting, not the minimum delta the suite can resolve statistically."
        },
        "confidence": {
          "type": "number",
          "exclusiveMinimum": 0,
          "exclusiveMaximum": 1
        },
        "sized_for": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "description": "A-priori sizing note: the item count this suite was built to reach, if it was sized deliberately. Null for suites whose size was chosen by what fits in seconds."
        }
      }
    },
    "significance_protocol": {
      "description": "The test this suite's outcomes may be subjected to, declared before any comparison is run. exact-mcnemar assumes INDEPENDENT paired binary outcomes; a suite whose cases are grouped by perturbation violates that assumption, and cluster-adjusted standard errors run materially larger than naive ones. The comparator refuses rather than reporting a number when the declared protocol does not match the data's structure -- a caveat beside a p-value gets the p-value quoted and the caveat dropped.",
      "enum": [
        "exact-mcnemar",
        "clustered-paired",
        "bootstrap-ci"
      ]
    },
    "derived_from": {
      "type": [
        "string",
        "null"
      ],
      "description": "For adversarial suites: the suite these cases perturb."
    }
  }
}
